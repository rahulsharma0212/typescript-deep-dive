export function add_number(x: number, y: number) {
    return x + y;
}

export function subtract_number(x: number, y: number) {
    return x - y;
}

const PI = 3.14;

function calculateArea(radius: number) {
    return PI * radius ** 2;
}

export default calculateArea;
